import numpy as np

# rng=np.random.default_rng()
# rng=np.random.default_rng(seed=1)  # seed reproduse same result

# print(rng.integers(low=1,high=50, size =5))
# print(rng.integers(low=1,high=50, size =(3, 2))) # set dimeantions

# floating num 
print(np.random.uniform(low=-1, high=1, size=(3, 2)))